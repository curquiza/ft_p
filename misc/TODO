- retour de tous les appels : listen, inet_addr, fork, accept, connect, fstat, dup2...
- checker les leaks
- checker les liens symboliques
- gerer les '\r\n' ?

SERVER:
- pq les leaks ??
- mode ASCII/BIN
- option verbose ?

CLIENT:
- gerer localhost
- le client doit verifier que le DT est ouvert avant de lancer des commandes type LIST
- reussir à faire que le client se ferme quand le server s'eteint ?

DONE:
- gerer un maximum de client connecté ? => gerer les signaux
- est ce que se connecter sur le server marche depuis une autre machine sur le même réseau ? Oui grace au ANYADRESS
- changer le verbose
- la creation du canal de données
- la fermeture du canal de donnée ? Oui, fait
- gerer les arguments du LS
- LIST : si erreur de syntax, close quand mm la connexion du DT
- mettre des beaux [ERROR] à la place de Error
